# Local issue mirror — generated by tools/gh_sync.py
.github_issues/

# Local drafts — never committed
knowledge/medium_draft_*.md

# Local clone of Make-AI-Agents — pulled fresh, never tracked
make-ai-agents/

# Local clone of handoff — pulled fresh, never tracked
handoff/

# Local agent context — not shared with the repo
AGENTS.md

# Local secrets / config — never committed
.env

# Handoff docs — local-only audit trail, never committed
handoffs/
